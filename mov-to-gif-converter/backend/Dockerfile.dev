FROM node:20-alpine

# Install FFmpeg
RUN apk add --no-cache ffmpeg

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Create directories
RUN mkdir -p uploads output temp

EXPOSE 3001

CMD ["npm", "run", "dev"]
